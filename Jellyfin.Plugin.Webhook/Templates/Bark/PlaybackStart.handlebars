{
"title": "PlaybackStart",
{{~#if_exist level~}}
    "level": "{{level}}",
{{~/if_exist~}}
{{~#if_exist icon~}}
    "icon": "{{icon}}",
{{~/if_exist~}}
{{~#if_exist group~}}
    "group": "{{group}}",
{{~/if_exist~}}
{{~#if_exist isArchive~}}
    "isArchive": "{{isArchive}}",
{{~/if_exist~}}
{{~#if_exist url~}}
    "url": "{{url}}",
{{~/if_exist~}}
"body": "User: {{#each data.Users}}{{{this.Username}}},{{/each}}\n Playing: {{data.Item.Name}}\n Client: {{{data.ClientName}}}\n Device: {{{data.DeviceName}}}\n Server: {{{data.ServerName}}}"
}